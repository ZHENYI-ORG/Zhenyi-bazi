# v1.1.0 — 真一判盘

- 在完整 TypeScript 源码层新增 `BlindJudgmentEngine`，不再修改编译后的 `dist` 作为源码。
- `/api/paipan` 自动附加 `data.blind_judgment`。
- 新增 `POST /api/judgment`，用于已排命盘补算判盘。
- 新增事实图谱、关系语义、上下文宾主、十神意向、气势、动态体用、做功路径、成果归属、正反局、主线裁决、岁运触发和证据链。
- 新增白色主题“判盘”结果页面，兼容手机和 PC。
- 新增判盘专项测试及 120 盘压力测试。
- 保留原排盘、真太阳时、神煞、四柱直排、保存命盘和 AI 入口行为。
